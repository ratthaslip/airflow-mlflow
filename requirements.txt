# -- Core Airflow --
apache-airflow==2.8.3

# -- Python Libraries for DOCX Processing DAG --
python-docx>=1.1.0,<1.2.0
# NOTE: pandas is already provided by the apache/airflow:2.8.3 base image
# (pinned to 2.1.4 by Airflow's official constraints). Do NOT re-pin it to an
# incompatible range or the image build will fail dependency resolution.
pandas>=2.0.0,<2.2.0

# -- Machine Learning Pipeline (flood_ml_pipeline) --
# scikit-learn 1.3.x ships the DecisionTreeClassifier / GridSearchCV APIs used
# by the notebook and is compatible with the Airflow 2.8.3 dependency set.
scikit-learn>=1.3.0,<1.4.0
joblib>=1.3.0,<1.4.0
pyarrow>=14.0.0,<16.0.0          # parquet engine for inter-task artifact passing

# -- Database Connectors & Common SQL --
pymysql>=1.1.0,<1.2.0
psycopg2-binary>=2.9.0,<2.10.0
apache-airflow-providers-common-sql

# -- Airflow Providers --
apache-airflow-providers-postgres
apache-airflow-providers-mysql
apache-airflow-providers-microsoft-azure
apache-airflow-providers-amazon

# -- Cloud SDKs (Dependencies for Providers) --
azure-identity
azure-storage-blob
boto3

# -- Other Utility Libraries --
requests>=2.31.0,<2.32.0
python-dotenv>=1.0.1,<1.1.0
beautifulsoup4>=4.12.0,<4.13.0

# -- Development Tools (Optional for Production) --
pylint>=3.0.0,<3.2.0
